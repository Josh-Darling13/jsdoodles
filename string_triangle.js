/*
A simple program with an output of strings 
in the shape of a trianlge.
*/


const foo = "#";
let repeatedString = "";
let n = 0;

while (n < 8) {
  repeatedString += foo;        
  console.log(repeatedString);
  n++;
};